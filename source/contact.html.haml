---
title: Contact us
keywords: contact, form
skip_side_menu: true
---
.container
  .row
    .col-lg-6
      = site_partial('custom_contact')
      %h1 Main Address
      %p= site_partial 'address'
      %h2 Email
      %p= email_form
      .alert.alert-success
        %h4 When you contact us please include:
        %ul
          - site.contact_form.fields.each do |field|
            %li= field
      %h2 
        Phone
      %p= link_to site.phone, "tel:#{site.phone}", class: 'btn btn-success btn-block'
      
    .col-lg-6
      - if site.geo_embed_address
        %iframe(width="600" height="450" frameborder="0" style="border:0" src="https://www.google.com/maps/embed?#{site.geo_embed_address}")